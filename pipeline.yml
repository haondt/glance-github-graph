tasks:
  - type: docker-build
    image: glance-github-graph
    platforms:
      - linux/arm64
      - linux/amd64
    auto:
      - source: push
        has_tag: true
        tag_source: tag
      - source: push
        has_tag: true
        tag_source: latest
    context: .
    registries:
      - gitlab
      - docker-hub
